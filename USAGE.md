# Usage

Use `scripts/walter run-safe` for bounded Codex CLI work. Always provide:

- a specific goal;
- target git project path;
- allowed write globs;
- forbidden paths when relevant;
- verification command.

No receipt means no result. Codex final text is not proof. Proof is the runner evidence bundle plus safety gate.
